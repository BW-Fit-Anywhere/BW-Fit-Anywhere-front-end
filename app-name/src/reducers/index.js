// import {  } from "../actions";

const initialState = {
  username: "",
  password: ""
}

export const reduser = (state = initalState, action) => {
  switch (action.type) {

    // cases go here
    
    default:
      return state
  }
}